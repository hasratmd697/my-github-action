name: Caching Demo Workflow # You can name the workflow anything

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # This is the step the checker is looking for!
      - name: 24f1002299@ds.study.iitm.ac.in
        run: echo "This step has the required name."

      # You can have other steps too
      - name: Checkout code
        uses: actions/checkout@v3
